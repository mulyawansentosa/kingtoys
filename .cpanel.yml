deployment:
  tasks:
    - export DEPLOYPATH=/home/kingtoys/public_html
    - /bin/mkdir -p "$DEPLOYPATH"
    - /usr/bin/rsync -a --exclude=".git/" --exclude=".cpanel.yml" /home/kingtoys/repositories/kingtoys/ "$DEPLOYPATH"/
